//in the file math.js add a function to divide two numbers at the end of the file. the function should take two numbers as arguments (e.g., a and b) and return the result of their division (e.g. a/b). Commit your changes;

const addNumbers = function(a, b) {
  return a + b;
};

const subtractNumbers = function(a, b) {
  return a - b;
};

console.log(addNumbers());

console.log(subtractNumbers());

console.log("hello");

const divideNumbers = function(a, b) {
  return a / b;
};

console.log(divideNumbers(10, 5));

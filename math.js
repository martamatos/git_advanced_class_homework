const addNumbers = function(a, b) {
  return a + b;
};

const subtractNumbers = function(a, b) {
  return a - b;
};

console.log(addNumbers());

console.log(subtractNumbers());

//function that divides two numbers, added on June 3, 2019
const divideNumbers = function(a, b) {
  return a / b;
};

console.log(divideNumbers());

// added with last update by Marta
console.log("hello");
